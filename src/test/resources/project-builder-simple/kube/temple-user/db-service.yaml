apiVersion: v1
kind: Service
metadata:
  name: temple-user-db
  labels:
    app: temple-user
    kind: db
spec:
  ports:
  - name: db
    port: 5432
    targetPort: 5432
  selector:
    app: temple-user
    kind: db
