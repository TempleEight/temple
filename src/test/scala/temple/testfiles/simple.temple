SimpleTempleTest: project {}

User: service {
  username: string;
  email: string(40,5);
  firstName: string;
  lastName: string;
  createdAt: datetime;
  numberOfDogs: int;
  yeets: bool @unique @server;
  currentBankBalance: float(min: 0.0, precision: 2);
  birthDate: date;
  breakfastTime: time;

  Fred: struct {
    field: string;
  };

  #auth(login: username);
  #uses [Booking, Events];
}
