SimpleTempleTest: project {
  username: string;
}

User: service {
  username: string;
  email: string(40,5);
  firstName: string;
  lastName: string;
  createdAt: datetime;
  numberOfDogs: int;
  yeets: bool;
  currentBankBalance: float(precision: 2);
  birthDate: date;
  breakfastTime: time;

  Fred: struct {
    field: string;
  };

  #auth(login: username);
  #uses [Booking, Events];
}
